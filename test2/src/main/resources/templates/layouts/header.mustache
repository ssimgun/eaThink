<!DOCTYPE HTML>
<!--
	Intensify by TEMPLATED
	templated.co @templatedco
	Released for free under the Creative Commons Attribution 3.0 license (templated.co/license)
-->
<html lang="ko">
<head>
    <title>Have a good meal</title>
    <meta charset="utf-8">
    <meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <script>
        const loggedInUser = {{# loggedInUser}} true {{/loggedInUser}}{{^ loggedInUser}} false {{/loggedInUser}};
    </script>
    <link rel="stylesheet" href="/assets/css/main.css">
    <!--    <link rel="stylesheet" href="/assets/css/chatstyle.css">-->
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Google Fonts Link For Icons -->
    <link rel="stylesheet"
          href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0"/>
    <link rel="stylesheet"
          href="https://fonts.googleapis.com/css2?family=Material+Symbols+Rounded:opsz,wght,FILL,GRAD@48,400,1,0"/>

</head>

<body>

<!-- Header -->
<header id="header">
    <!--    사이드바 버튼-->
    <nav class="left"><a href="#menu">
        <span>Menu</span></a>
    </nav>

    <!--    날씨 정보 칸-->
    <nav class="image">
        <div class="weather-info" id="weatherInfo">
        <span><img class="weather-image"
                   src="/images/weather/weather_{{weatherImageName}}.png"
                   alt="{{weatherImageName}}"></span>
        </div>
    </nav>

<nav class="left">
<div class="weather-details" id="weatherDetails">
    <p><img class="weather-image"
            src="/images/weather_{{weatherImageName}}.png"
            alt="{{weatherImageName}}"
            style="width:50%;"></p>
    {{#weather}}
        <p> 현재 {{secondName}}의 날씨</p>
        <p> 기온 : {{t1h}}도</p>
        <p> 습도 : {{reh}}%</p>
        <p> 강수량 : {{rn1}}</p>
    </div>
    </nav>
    {{/weather}}

    <!--    페이지 명-->
    <a href="/main" class="logo">식사를 합시다</a>

    <!--    로그인 버튼-->
    {{^loggedInUser}}
        <nav class="right">
            <a href="/loginpage" class="button alt" id="nav-login-btn">로그인</a>
        </nav>
    {{/loggedInUser}}
    {{#loggedInUser}}
        <nav class="right">
            <a href="/logout" class="button alt" id="nav-logout-btn">로그아웃</a>
        </nav>
    {{/loggedInUser}}
</header>

<!-- Menu -->
<nav id="menu" class="">
    <ul class="links">
        <li><a href="/main">홈페이지</a></li>
        <li><a href="/edit">내정보</a></li>
        <li><a href="#one">채팅</a></li>
        <!--        <li><a href="#three">게임</a></li>-->
        <li><a href="/surveySelect" id="food-preference-link">
            나의 음식 취향</a></li>
    </ul>

    {{^loggedInUser}}
        <ul class="actions vertical">
            <li><a href="/loginpage" class="button fit" id="side-login-btn">로그인</a></li>
        </ul>
    {{/loggedInUser}}

    {{#loggedInUser}}
        <ul class="actions vertical">
            <li><a href="/logout" class="button fit" id="side-logout-btn">로그아웃</a></li>
        </ul>
    {{/loggedInUser}}

    <!--개인 프로필 추가하기 -->
    <!--로그인 했을 때만 띄어야 함-->
    <!--    날씨 데이터 표시-->

    <!--        유저 정보 표시-->
    <!--<div class="flex-item nav-under">-->
    <!--    <#= 데이터가 있을 때>-->
    {{#loggedInUser}}
        <ul class="links-under nav-under">
            <li style="border-bottom : 1px solid; padding-bottom: 10px;">{{nickname}}님 반갑습니다!</li>
            <li>
                <div class="dropdown-myAddresses">
                    <div>
                        나의 주소
                    </div>
                    <div class="dropdown">
                        {{#selectAddress}}
                            <button class="dropbtn" onclick="dropdown()">
                                <span class="dropbtn_content">{{address_name}}</span>
                                <input id="addressHidden" type="hidden" value="{{address}}">
                            </button>
                        {{/selectAddress}}
                        {{^selectAddress}}
                            <button class="dropbtn" onclick="dropdown()">
                                <span class="dropbtn_content">주소 선택</span>
                            </button>
                        {{/selectAddress}}
                        <div class="dropdown-content">
                            {{#addresses}}
                                <div class="myAddress" onclick="selectAddress('{{address_name}}','{{id}}')">
                                    {{address_name}}<br>({{address}})
                                </div>
                            {{/addresses}}
                        </div>
                    </div>
                </div>
            </li>
            <li class="button-container">
                <a type="button" href="/myInfo/edit">내 정보</a>
                <a type="button" href="/myInfo/addressList">주소목록</a>
            </li>
        </ul>
    {{/loggedInUser}}

    <!--    <^ = 데이터가 없을 때>-->
    <!--{{^loggedInUser}}
    <ul class="links-under nav-under">
        <li><a href="내정보">내 정보</a></li>
        <li> </li>
        <li style="display: flex; gap: 10px;">
            <button href="주소목록">내 정보</button>
            <button href="주소목록">주소목록</button>
        </li>

    </ul>
{{/loggedInUser}}-->
    <!--</div>-->
</nav>
